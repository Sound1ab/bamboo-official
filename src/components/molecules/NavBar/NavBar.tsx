import * as React from 'react'
import { Container, Heading } from '../../atoms'

interface PropTypes {}

export const NavBar = (props: PropTypes) => (
  <Container>
    <Heading type="h4">Products</Heading>
  </Container>
)
